{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test:unit": "jest --testMatch '**/tests/unit/**/*.test.js' --watchAll=false",
    "test:staging": "jest --testMatch '**/tests/staging/**/*.test.js' --watchAll=false"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "supertest": "^7.1.4"
  }
}
